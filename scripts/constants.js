/**-----------------------------------------------------------------------------------game object */ 
/**
 * const game = {
    inventory: ["potion"],
    items: [],
    room: [], 
    monster: [],
    attack: [],
    health : [],
    monsterHealth : [],
    score : 0
};

/**-----------------------------------------------------------------------------------game buttons */ 
/*
const nextRoomButton = document.getElementById("next-room");
const attackButton = document.getElementById("attack");
const blockButton = document.getElementById("block");
const potionButton = document.getElementById("drink-potion");

const BLOCK = 1;
const ATTACK = 2;
const HIT = 3;
const attackToMessageMapper = {
    BLOCK : "Blocks",
    ATTACK : "Attacks",
    HIT : "Hits",
}

function fight (){
    let monsterAttack = Math.floor(Math.random()*3)+1;
    game.attack.push(`Monster ${attackToMessageMapper [monsterAttack.valueOf]}`);
    return game.attack;
        
}

const gameInventoryScoreMapper = {
    "diamond ring": 200,
    "bag of gold coins": 150,
    "bag of silver coins": 50,
    "bronse statue": 20,
    "bag of jemstones": 500,
    "crystal dagger": 30,
    "old pair of socks": 0,

};
*/